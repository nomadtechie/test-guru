import * as api from './api';

describe('api', () => {

    test('non stubbed', () => {

    });
    // 1: stubbing a promise

    // 2: stubbing async await

    // 3: stubbing an error in promise

    // 4: stubbing an error from async/await

    // 5: now make it useful - stub an async method which is inside another method

    // 6: now make it more useful - stub
});